spring:

  # dev-tools
  devtools:
    restart:
      enabled: true

  # database
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/demo_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: LXY20050720

  # Redis
  data:
    redis:
      host: localhost
      port: 6379
      password:
      # Lettuce pool
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: 1000ms

#JWT
jwt:
  secret: mySecretKeyThatIsLongEnough32Bytes_ChangeMeInProduction
  expiration: 86400000

# MyBatis Plus Log
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl